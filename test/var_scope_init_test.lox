// Test for variable initialization in different scopes
print "Testing variable scope initialization...";

var global = "global";
print global;  // Should work fine

{
  var a; // Declared but uninitialized
  var b = "initialized";
  print b;  // Should work fine
  
  // This should throw an error
  print a;
  
  // Code below shouldn't execute
  a = "now initialized";  
  print a;  // Would work if we reached here
}
