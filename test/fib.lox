/**
* fib'in around :)
*/

fun fib(n) {
    if (n <= 1) return n;

    return fib(n-1) + fib(n-2);
}

fun main() {
    for (var i = 0; i < 20; i++) {
        print "fib(" + i + "): " + fib(i);
    }
}

main();
