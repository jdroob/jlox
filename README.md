### To Run
```
# Because I'm lazy... just make jlox simple to run
alias jlox='java -cp bin com.craftinginterpreters.lox.Lox'

# Then you're good to go!
jlox /path/to/<filename>.lox

# Or if you'd like to run the REPL...
jlox
```